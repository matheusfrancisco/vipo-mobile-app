const initialState = {
  name: '',
  address: '',
  email: '',
};

const reducer = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default reducer;